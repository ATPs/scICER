% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering_resolution_search.R
\name{find_resolution_ranges}
\alias{find_resolution_ranges}
\title{Find resolution parameter ranges for each cluster number using binary search}
\usage{
find_resolution_ranges(
  igraph_obj,
  cluster_range,
  start_g,
  end_g,
  objective_function,
  resolution_tolerance,
  n_workers,
  verbose,
  seed = NULL,
  snn_graph = NULL,
  min_cluster_size = 1L,
  in_parallel_context = FALSE,
  runtime_context = NULL
)
}
\arguments{
\item{igraph_obj}{igraph object to cluster}

\item{cluster_range}{Target cluster numbers to search}

\item{start_g}{Lower bound of search interval (log scale for CPM path)}

\item{end_g}{Upper bound of search interval (log scale for CPM path)}

\item{objective_function}{Objective function ("modularity" or "CPM")}

\item{resolution_tolerance}{Resolution convergence tolerance}

\item{n_workers}{Requested worker count}

\item{verbose}{Whether to print progress diagnostics}

\item{seed}{Optional deterministic seed}

\item{snn_graph}{Seurat SNN graph matrix for final best-label merging}

\item{min_cluster_size}{Minimum required cells per effective cluster count}

\item{in_parallel_context}{Whether called from an outer parallel worker}

\item{runtime_context}{Internal mutable context for spill/memory controls}
}
\value{
Named list mapping cluster number to \code{c(left_bound, right_bound)}
}
\description{
Estimates lower/upper resolution bounds that reproduce each target effective cluster count.
}
\details{
For each requested \code{k}, this helper runs two binary searches:
one for the smallest gamma yielding at least \code{k} effective clusters and
one for the largest gamma yielding at most \code{k} effective clusters
(CPM/modularity aware).
Effective clusters are those with size \code{>= min_cluster_size}; all-small
trials are counted as \code{0}.

Large graphs use lighter preliminary settings to reduce runtime pressure.
After per-k search, adjacent ranges are lightly adjusted when nearly touching
to reduce ambiguity between neighboring target counts.
}
\keyword{internal}
