% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_ic}
\alias{plot_ic}
\title{Plot Inconsistency (IC) scores across different cluster numbers}
\usage{
plot_ic(
  scice_results,
  threshold = 1.005,
  figure_size = c(10, 6),
  title = "Clustering Consistency Analysis",
  show_threshold = TRUE,
  show_gamma = TRUE
)
}
\arguments{
\item{scice_results}{Results object from scICE_clustering function}

\item{threshold}{IC threshold line to display (default: 1.005)}

\item{figure_size}{Figure size as c(width, height) (default: c(10, 6))}

\item{title}{Plot title (default: "Clustering Consistency Analysis")}

\item{show_threshold}{Whether to show threshold line (default: TRUE)}

\item{show_gamma}{Whether to add selected gamma values below each x-axis
cluster label with a 45 degree rotation (default: TRUE)}
}
\value{
ggplot object
}
\description{
Creates a boxplot showing the distribution of IC scores for each cluster number 
tested. Lower IC scores indicate more consistent clustering results.
}
\details{
\code{plot_ic()} is typically the first diagnostic plot after
\code{scICE_clustering()}:
\itemize{
  \item each x-position is a returned final merged cluster number;
  \item box/jitter values are bootstrap IC scores (\code{ic_vec});
  \item the threshold line marks your chosen consistency cutoff.
}

If no valid IC values are available (for example all targets excluded),
the function returns an informative empty plot instead of failing.
}
\examples{
\dontrun{
# Run scICE analysis
scice_results <- scICE_clustering(pbmc_small, cluster_range = 2:10)

# Plot IC results
plot_ic(scice_results)

# Customize plot
plot_ic(scice_results, threshold = 1.01, title = "My Clustering Analysis")
}
}
