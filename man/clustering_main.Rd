% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering_core.R
\name{clustering_main}
\alias{clustering_main}
\title{Core clustering algorithm implementing binary search and optimization}
\usage{
clustering_main(
  igraph_obj,
  cluster_range,
  n_workers = max(1, parallel::detectCores() - 1),
  n_trials,
  n_bootstrap,
  seed = NULL,
  beta,
  n_iterations,
  max_iterations,
  objective_function,
  remove_threshold,
  snn_graph = NULL,
  min_cluster_size = 1L,
  resolution_tolerance,
  verbose,
  in_parallel_context = FALSE,
  runtime_context = NULL,
  precomputed_gamma_dict = NULL,
  precomputed_resolution_search_diagnostics = NULL,
  precomputed_coverage_complete = NULL
)
}
\arguments{
\item{igraph_obj}{igraph object to cluster}

\item{cluster_range}{Vector of cluster numbers to test}

\item{n_workers}{Number of parallel workers (default: max(1, parallel::detectCores() - 1))}

\item{n_trials}{Number of clustering trials per resolution}

\item{n_bootstrap}{Number of bootstrap iterations}

\item{seed}{Optional deterministic seed}

\item{beta}{Beta parameter for Leiden clustering}

\item{n_iterations}{Number of Leiden iterations}

\item{max_iterations}{Maximum iterations for optimization}

\item{objective_function}{Objective function ("modularity" or "CPM")}

\item{remove_threshold}{Threshold for removing inconsistent results}

\item{snn_graph}{Seurat SNN graph matrix for final small-cluster merge}

\item{min_cluster_size}{Minimum required cells per effective cluster count}

\item{resolution_tolerance}{Tolerance for resolution parameter search}

\item{verbose}{Whether to print progress messages}

\item{in_parallel_context}{Whether this function is called from within a parallel context (default: FALSE)}

\item{runtime_context}{Internal mutable context for spill/memory controls}
}
\value{
List with clustering results
}
\description{
Internal engine that executes the end-to-end scICER clustering workflow.
}
\details{
Major stages:
\enumerate{
  \item clear cache and initialize run state;
  \item search feasible resolution ranges per target cluster number;
  \item optionally filter unstable targets (skipped when threshold is \code{Inf});
  \item optimize each valid target via repeated Leiden trials and bootstrap IC;
  \item merge successful/excluded entries into a compatibility-preserving result list.
}

The function includes extensive defensive handling for empty/failed branches,
making it safe for large parameter sweeps where some targets may fail to
converge.
}
\keyword{internal}
